#include "ecsfy/World.hpp"
#include "ecsfy/Registry.hpp"
#include "catch2/catch.hpp"
#include "EntityTraits.hpp"

TEST_CASE("world", "[world]") {
  // using ComponentRegistry = ecsfy::ComponentRegistry<int, float, double>;
  // using SystemRegistry = ecsfy::SystemRegistry<int, float, double>;
  // using World = ecsfy::World<EntityTraits, ComponentRegistry, SystemRegistry>;

  // STATIC_REQUIRE( World::ComponentCount == 3 );
  // STATIC_REQUIRE( World::SystemCount == 3 );
}
